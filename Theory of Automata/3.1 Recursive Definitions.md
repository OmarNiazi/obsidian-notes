The Recursive Definition is characteristically a **3-step process**. It is as follows:

1) First, we specify some basic objects in the set
2) Second, we give rules for constructing more objects in the set from the base ones
3) Third, we declare that no objects except those constructed in this way are allowed in the set

**NOTE:** This 
